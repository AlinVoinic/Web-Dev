<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit product</title>
</head>
<body>
    <h1>Edit the product:</h1>
    <form action="/products/<%=oldProduct._id%>?_method=PUT" method="post"> 
        <label for="name">Name</label>
        <input type="text" id="name" name="name" value="<%=oldProduct.name%>">
        <label for="price">Price</label>
        <input type="text" id="price" name="price" value="<%=oldProduct.price%>">
        <label for="category">Category</label>
        <select name="category" id="category">
            <% for (let category of categories) { %>
                <option value="<%=category%>" <%= oldProduct.category === category ? 'selected' : '' %>> 
                    <%= category %> 
                </option>   
            <% } %>  
        </select>
        <br>
        <button>Submit</button>
    </form>
    <a href="/products">Back to all products!</a>
</body>
</html>